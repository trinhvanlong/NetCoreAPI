name: Deploy .NET 10 MVC to MonsterASP.net

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:

      # 1. Lấy source code từ GitHub
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Cài đặt .NET 10 SDK
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # 3. Restore NuGet packages
      - name: Restore
        run: dotnet restore DemoMVC/DemoMVC.csproj

      # 4. Build project
      - name: Build
        run: dotnet build DemoMVC/DemoMVC.csproj -c Release --no-restore

      # 5. Publish project
      - name: Publish project
        run: dotnet publish DemoMVC/DemoMVC.csproj -c Release --no-build -o ./publish

      # 6. Tạo app_offline.htm để báo website đang cập nhật
      - name: Stop Server
        run: |
          echo "<html><body><h1>Website đang cập nhật...</h1></body></html>" > app_offline.htm
          curl -s --ftp-pasv \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            -T app_offline.htm \
            "ftp://${{ secrets.FTP_SERVER }}/wwwroot/app_offline.htm"

      # 7. Upload website lên MonsterASP
      - name: Deploy App
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftp
          local-dir: ./publish/
          server-dir: /wwwroot/
          security: loose
          exclude: |
            **/app_offline.htm

      # 8. Xóa app_offline.htm để website hoạt động lại
      - name: Start Server
        if: always()
        run: |
          curl -s --ftp-pasv \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://${{ secrets.FTP_SERVER}/" \
            -Q "-DELE /wwwroot/app_offline.htm" || true